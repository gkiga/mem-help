<h1>My Videos</h1>

<% videos.each do |video| %>
    <p>
        <strong>Title:</strong>
        <%= video.title %>
        <%= link_to 'Watch', video_path(video)%>
    </p>

<% end %>

<%= link_to 'Add Video', new_account_video_path, :class => "btn btn-primary text-center" %>
