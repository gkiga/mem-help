<body>

<br>
<br>
<h1 class="text-center", style="color:#ed2f2f;">My Messages Page</h1>
<br>

<p class="text-secondary">You can send a request to another student by searching for them on the  homepage and clicking "Direct Request" on their profile! </p>
<br>
<p class="text-secondary">If you want a closer look at your requests, click "See Request".</p>

<br>
<h3><p class="text-primary">My Sent Requests</p></h3>
<% messages.each do |message| %>
<% if current_user.try(:id)==message.user_id %>
  
  <div id="<%= dom_id(message) %>">
    <div class="container card p-2 mb-2 bg-light text-dark">
 <div class = "text-center">
  <div class="header">
    <h2>Message To: <%= message.recipient_name%></h2>
    See <%= message.recipient_name%>'s
    <a><%= link_to 'Message', message_path(message.recipient) %></a>
  </div>
  <%end%>
  <%end%>